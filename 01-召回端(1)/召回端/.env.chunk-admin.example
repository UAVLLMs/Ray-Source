# Chunk 管理路由开关；设置为 0 可完全禁用写入管理能力。
CHUNK_ADMIN_ENABLED=1

# 受限 NL2SQL 只读查询开关；不会直接修改 JSON 或检索索引
CHUNK_SQL_ENABLED=1

# 一键切分默认使用以下参数；管理页面和 CLI 可以逐次覆盖。
CHUNK_TARGET_CHARS=720
CHUNK_MIN_CHARS=160
CHUNK_MAX_CHARS=1100
CHUNK_OVERLAP_CHARS=90
