{
  "name": "ragv6-web-client",
  "version": "1.0.0",
  "private": true,
  "description": "Dependency-free web client and API gateway for the standalone RAGv6 retrieval service",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check channel-concurrency.js && node --check shared-state.js && node --check recommended-answer-cache.js && node --check public/ragv6-ui/audit-contract.js && node --check public/ragv6-ui/app.js && node --check public/chunk-manager/app.js && node --test audit-contract.test.js channel-concurrency.test.js shared-state.test.js recommended-answer-cache.test.js production-invariants.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "redis": "^4.7.0"
  }
}
